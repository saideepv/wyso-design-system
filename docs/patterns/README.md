# Patterns

Recurring flows assembled from components — onboarding, the daily mission
loop, the conversation/quiz loop, streaks & rewards, etc.

Nothing documented yet. Add one doc per pattern once a flow has been
designed end-to-end, rather than as a single screen.
