# Components

Inventory of components that exist (in prototype form) vs. documented.
None are implemented as real code components yet — this tracks what's
been designed so it doesn't get lost.

## Core

| Component | Status |
| --- | --- |
| Button (primary / secondary / soft / ghost / disabled) | designed in prototype |
| Chip | designed in prototype |
| ProgressBar | designed in prototype |
| Blob (avatar / icon holder / accent) | designed in prototype |

## Conversation

| Component | Status |
| --- | --- |
| ChatBubble (companion / student / correct / retry) | designed in prototype |
| Waveform | designed in prototype |
| Sparks counter | designed in prototype |
| MissionCard | designed in prototype |
| CelebrationToast | designed in prototype |
| MicButton | designed in prototype |

Each row should eventually become its own doc with anatomy, states,
accessibility notes, and a link to its implementation.
