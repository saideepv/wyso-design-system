# Figma

Links to the source Figma file(s), plus any exported style-dictionary
JSON if/when we set up token sync between Figma and `tokens/`.

Nothing here yet — add the file link as soon as one exists.
